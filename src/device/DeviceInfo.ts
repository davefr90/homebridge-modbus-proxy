/**
 * Static metadata describing a device.
 */
export interface DeviceInfo {

  /**
   * Manufacturer name.
   */
  manufacturer: string;

  /**
   * Device model.
   */
  model: string;

  /**
   * Human-readable device name.
   */
  name: string;

}